import '../styles/TitleBar.css';

// Title bar that is perpetually at the top of the screen, says LMNT
const TitleBar = () => {

    return (
        <div className="title">
            LMNT
        </div>
    )
}